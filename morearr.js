const numbers = [22, 33, 44, 55, 66, 77, 88, 99];
const newNumber = numbers.map(number => number * 2);
console.log(newNumber);
/**
 * Write summary about array methods map, filter , find, forEach
 * forEach()- is used to excute the same code on every element in an array but does not change the array & it return undefiend
 * 
 * map()-excutes the same on every element in an array and returns a new array wtih the updated element.
 * 
 * filter()- method is used to filters all the elements and returns the element that matches and the element that do not match are removed.
 * 
 * find()- is used to  find all the descendant elements of the selected element. It finds the element in the DOM tree by traversing through the root leaf
 */
/**
 * Write about ternay operator .
 * ternary operator is shortcut of if-else
 */
let x = 10;
const ternary = x <= 10 ? 'sotti' : 'Mitta';
console.log(ternary);