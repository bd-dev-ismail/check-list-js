let a = 30;
a = 20;
// console.log(a);
const b = 50;
// b= 40;
console.log(b);
/**
 * Var
 * var1- Value can be overwrite or reassign possiable
 * var2- Same variabel re declare possible.
 * var3- var is functional scope.
 * let
 * let1-Also same as var like value reassign possible.
 * let2-No possible to re delare same variabel. 
 * let3-let is block scope
 * const - constant vaiabel
 * const-Can't overwitre or reassign any variabel & can't re declare it.Block scope.
 */